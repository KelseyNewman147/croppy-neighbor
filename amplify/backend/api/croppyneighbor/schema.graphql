type Crop @model {
  id: ID!
  common_name: String
  scientific_name: String!
  pests: [String]
  rooting_depth: Int
  foot_print: Int
  light_needs: Int
  water_needs: Int
  nutrient_needs: Int
  time_in_field: Int
  family: String
  labor: Int
  profit: Int 
}
